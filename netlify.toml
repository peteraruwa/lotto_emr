[build]
  base      = "apps/web"
  command   = "cd ../.. && pnpm install --frozen-lockfile && pnpm turbo run build --filter=@lotto-emr/web"
  publish   = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
